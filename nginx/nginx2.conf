events {}
 
http {
    server {
        listen 80 default_server;

        location / {
            root /api/fro
        }

        location /api/ {
            proxy_pass http://127.0.0.1:8000;
        }
        
    }
}

