{
  "name": "home-reporting-tool",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start:dev": "concurrently \"npm:dev-backend\" \"npm:dev-frontend\"",
    "dev-backend": "cd backend && cp .env.example .env && ./venv/bin/python -m uvicorn main:app --reload",
    "dev-frontend": "cd frontend && cp .env.example .env && npm install && npm run build && npm run dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
