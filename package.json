{
  "name": "home-reporting-tool",
  "version": "1.0.0",
  "description": "Currently being developed into BETA stage. This is a home reporting tool for documenting properties and saving results to a database connected to your specific user. Setting up is simple and currently supported on linux/mac OS/wsl on Windows.",
  "main": "ecosystem.config.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "docker-setup": "concurrently --kill-others \"npm run start-backend\" \"npm run start-frontend\" \"npm run start-nginx\"",
    "local-setup": "concurrently --kill-others \"npm run start-backend-local\" \"npm run start-frontend-local\"",
    "start-nginx": "sleep 40 && nginx -g 'daemon off;'",
    "start-backend": "cd backend && uvicorn main:app --host 0.0.0.0 --port 8000 --reload",
    "start-backend-local": "cd backend && bash -c 'source ./venv/bin/activate && uvicorn main:app --reload'",
    "start-frontend": "cd frontend && npm run start -- -H 0.0.0.0 -p 3000",
    "start-frontend-local": "cd frontend && npm run dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^9.2.1"
  }
}
